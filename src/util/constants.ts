import { resolve as path_resolve } from "path";

export const DATA_TMP_DIR = path_resolve(__dirname, "../../tmp");
export const DATA_TCX_DIR = path_resolve(__dirname, "../../data_tcx");
export const DATA_JSON_DIR = path_resolve(__dirname, "../../data_json");
export const DATA_META_DIR = path_resolve(__dirname, "../../data_meta");
